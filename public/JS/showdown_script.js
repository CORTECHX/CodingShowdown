var url_string = window.location.href;
var url = new URL(url_string);
var id = url.searchParams.get("id"); //id of the match to be displayed
console.log(c);

///get the video feeds from firebase and display the match